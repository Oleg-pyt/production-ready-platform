{
  "openapi": "3.1.1",
  "info": {
    "title": "Production Ready Platform API",
    "version": "1.0.0",
    "description": "REST API for Production Ready Platform with generated TypeScript and Java clients"
  },
  "tags": [
    {
      "name": "Health",
      "description": "Service health and status checks"
    },
    {
      "name": "Users",
      "description": "User management operations"
    },
    {
      "name": "System",
      "description": "System and configuration endpoints"
    }
  ],
  "paths": {
    "/api/health": {
      "get": {
        "tags": [
          "Health"
        ],
        "summary": "Health check",
        "description": "Returns the current health status of the service",
        "operationId": "getHealth",
        "responses": {
          "200": {
            "description": "Service is healthy",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "HealthResponse": {
        "type": "object",
        "description": "Health status response",
        "properties": {
          "status": {
            "type": "string",
            "example": "UP",
            "description": "Service status (UP, DOWN, or custom states)"
          }
        },
        "required": [
          "status"
        ]
      }
    }
  }
}



